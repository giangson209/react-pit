import React from 'react';

type Props = {};

const TagInstallment = (props: Props) => {
  return <span className="tag tag-vector tag-md h-auto bg-gradient-to-r from-yellow-500 to-red-500 py-2">Trả góp 0đ</span>;
};

export default TagInstallment;
