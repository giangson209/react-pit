import HeaderWebDefault from '../header/header-web-default';

type Props = {};

const ModalPackInformation = (props: Props) => {
  return (
    <div>
      <HeaderWebDefault title="Gói cước tháng PARTY149" type="fixed" withMenu />
    </div>
  );
};

export default ModalPackInformation;
