const BASE = 'base';

const PRIMARY = 'primary';
const SECONDARY = 'secondary';
const TERTIARY = 'tertiary';
const GHOST = 'ghost';

const prefixByBrand = {
  primary: 'p',
  secondary: 's',
  tertiary: 't',
  ghost: 'g'
};

module.exports = { prefixByBrand, PRIMARY, SECONDARY, TERTIARY, GHOST };
